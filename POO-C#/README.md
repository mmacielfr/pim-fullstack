# Aluguel Moto (console)

Requer [.NET 8 SDK](https://dotnet.microsoft.com/download) instalado.

```bash
cd POO-C#
dotnet run
```

O `dotnet build` recria as pastas `bin` e `obj` localmente; elas não entram no Git.
